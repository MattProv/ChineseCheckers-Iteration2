package org.example.message;

/**
 * Enum class used to define the type of the message
 */
public enum MessageType
{
    STRING,
    DISCONNECT,
    MOVE,
    COMMAND,
    GAMESTATE,
    USERNAME,
    USERLIST,
    RULES_TYPE,
    BOARD_TYPE,
    END_TURN,
    PROMPT_MOVE,
}